class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        #solution 1
        return len(nums) != len(set(nums))
        #solution 2
        '''hashset=set()
        for n in nums:
            if n in hashset:
                return True;
            hashset.add(n)
        return False'''
        
        
